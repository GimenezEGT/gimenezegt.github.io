# Jekyll build artifacts
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Ruby / Bundler
.bundle/
vendor/
*.gem

# Node (if used for tooling later)
node_modules/

# OS / editor
.DS_Store
Thumbs.db
*.log
*.tmp
*.swp

# Claude Code — personal/local settings (permission grants); keep out of VC
.claude/settings.local.json

# oh-my-claudecode — local tool/session state; keep out of VC
.omc/
